# تقرير التدقيق الرجعي وإصلاح وحدة المخالفات

**الإصدار المدقق:** النسخة الأمنية الأساسية مقابل الحزمة الأخيرة للمخالفات، ثم حزمة الإصلاح الحالية.  
**النطاق:** استعلامات المخالفات، قواعد Firestore، ملف المدير المحمي، وسلامة النشر.  
**مبدأ العمل:** لا تُعد نتيجة من وحدة المخالفات دليلاً على صلاحيات العميل؛ قواعد Firestore هي مصدر التفويض الفعلي، والاستعلام لا ينجح إلا إذا كانت كل نتيجة محتملة مسموحاً بها. [1]

## النتيجة التنفيذية

لم يظهر في المقارنة أي حذف حديث لتدفق تعديل المدير المحمي أو تغيير للبريد بسبب إضافة المخالفات. ظهر أن PIN كان موجوداً في المصدر الأول فقط، ثم أزيل **قبل** إصدار النسخة الأمنية الأساسية؛ بينما كانت النسخة الأمنية الأساسية والنسخة الأخيرة تتطلبان كلمة مرور Firebase الحالية وتسمحان بتعديل الاسم والهاتف فقط. لذلك فإن غياب PIN ليس تراجعاً أحدثته وحدة المخالفات، لكنه مطلب متوافق أعيد دعمه في حزمة الإصلاح الحالية مع إبقاء كلمة المرور شرطاً إلزامياً.

أما أعطال المخالفات، فالاستعلامات في الحزمة السابقة كانت مطابقة منطقياً لقواعد القراءة، وتعريفات الفهارس الستة المرفقة مطابقة لاستعلاماتها المركبة. لكن ظهور الرسائل العامة، بدلاً من رسالة الفهرس الخاصة التي كانت موجودة في الملف الأخير، لا يثبت نقص الفهارس؛ بل يرجح أحد أمرين: أن الموقع المنشور لا يطابق ملفات الحزمة الأخيرة، أو أن الخطأ من نوع آخر مثل `permission-denied`. كما أن نجاح **All** يستبعد فشل تسجيل الدخول أو دور المدير كسبب عام. لا يمكن الجزم بالرمز الفعلي من دون الاتصال بمشروع Firebase الحي، ولذلك أزيل الاعتماد التشغيلي على تلك الفهارس في واجهات المستخدم بدلاً من التخمين.

| مجال التدقيق | النتيجة | المعالجة في حزمة الإصلاح |
|---|---|---|
| فلاتر المدير | كانت تجمع `where` مع `orderBy('violationAt','desc')` وتحتاج فهراً مركباً لكل فلتر. | تحولت إلى استعلام مساواة فقط ثم ترتيب محلي وعرض 10 سجلات في كل مرة. |
| مخالفات السيارة | كان استعلام المدير والمستخدم يستخدمان حقولاً متعددة مع ترتيب خادمي. | بقيت شروط السيارة/المستخدم/الحالة كما هي، لكن أزيل `orderBy` الخادمي والتصفح المعتمد على الفهرس. |
| My Violations | كان الاستعلام يطابق شرط قاعدة القراءة (`userId` و`AUTO_LINKED`) لكنه يعتمد على فهرس مركب للترتيب. | بقي مقيداً بالشرطين نفسيهما، ثم رتب محلياً وقُسّم إلى 10 سجلات. |
| قواعد القراءة | لم يوجد تعارض بنيوي بين شرط المستخدم والاستعلام المقيد به. | لم تُوسّع صلاحية المستخدم؛ ما زال لا يقرأ إلا مخالفاته المرتبطة تلقائياً. |
| PIN المدير | حُذف قبل النسخة الأمنية الأساسية؛ لم يكن في الحزمة الأخيرة. | أعيد كتأكيد إضافي متوافق، مع دعم PIN القديم المجزأ أو النصي للترحيل. |
| البريد الإلكتروني | لم يكن حقلاً قابلاً للتحرير في المصدر الأول أو النسخة الأمنية أو النسخة الأخيرة. | بقي معطلاً؛ لا يُكتب بريد Firebase Auth في Firestore فقط. |

## مقارنة ميزات المدير المحمي

| الميزة | المصدر الأول | النسخة الأمنية الأساسية | الحزمة الأخيرة قبل الإصلاح | حزمة الإصلاح |
|---|---|---|---|---|
| زر **Edit My Profile** للمدير | موجود | موجود | موجود | موجود |
| اشتراط كلمة المرور الحالية | موجود | موجود | موجود | موجود |
| PIN رباعي حالي | موجود | أزيل | أزيل | يعاد طلبه إن كان مخزناً |
| تغيير PIN | موجود | أزيل | أزيل | متاح؛ يخزن SHA-256 عند التغيير |
| تعديل الاسم والهاتف | موجود | موجود | موجود | موجود |
| تعديل البريد في النموذج | غير موجود | غير موجود | غير موجود | غير موجود عمداً |
| كتابة البريد في ملف Firestore فقط | غير آمن | ممنوع بالقواعد | ممنوع بالقواعد | ممنوع بالقواعد |

> **تنبيه أمني:** إعادة التحقق بكلمة مرور Firebase هي الحاجز الأمني الحقيقي. PIN رباعي داخل تطبيق متصفح وخطة Spark ليس عاملاً مستقلاً بالقوة ذاتها، لأن فضاءه لا يتجاوز 10,000 احتمال ولا يوجد خادم موثوق للتحقق منه. لذلك لا تسمح الحزمة بأي تعديل للمدير المحمي من دون كلمة المرور، حتى عند تفعيل PIN.

## التحقق من قواعد Firestore واستعلامات المخالفات

قاعدة `violations` تمنح المدير قراءة شاملة، وتمنح المستخدم النشط قراءة السجل فقط حين يكون `userId` مساوياً لمعرف جلسة المستخدم و`matchStatus` مساوياً لـ`AUTO_LINKED`. صفحات المستخدم في الإصلاح ترسل هذين الشرطين دائماً، ومع صفحة السيارة تضيف `carId` فقط. هذا يطابق مبدأ Firebase القائل إن قواعد الأمان ليست مرشحات للنتائج، بل يجب أن يقيد الاستعلام نفسه كل نتيجة محتملة بما يفي بشرط القاعدة. [1]

تدعم Firestore استعلامات المساواة المفردة تلقائياً، كما يمكنها دمج فهارس المساواة للاستعلامات المركبة من هذا النوع؛ بينما الجمع بين مساواة وترتيب على حقل آخر هو الشكل الذي يتطلب فهراً يدوياً عادةً. [2] لذلك تستخدم الحزمة المصلحة ما يلي:

| الواجهة | الاستعلام بعد الإصلاح | الترتيب والتقسيم |
|---|---|---|
| **All** للمدير | لا فلتر + `violationAt desc` | خادمي، 10 ثم **Load More**. |
| فلتر الحالة/التسوية للمدير | مساواة في `matchStatus` أو `settlementStatus` فقط | محلي، الأحدث أولاً، 10 ثم **Load More**. |
| مخالفة سيارة للمدير | مساواة في `carId` فقط | محلي، 10 ثم **Load More**. |
| مخالفة مستخدم للمدير | مساواة في `userId` فقط | محلي، 10 ثم **Load More**. |
| **My Violations** | مساواة في `userId` و`matchStatus=AUTO_LINKED` | محلي، 10 ثم **Load More**. |
| مخالفة مستخدم داخل سيارة | مساواة في `userId` و`matchStatus=AUTO_LINKED` و`carId` | محلي، 10 ثم **Load More**. |

احتُفظ بملف `firestore.indexes.json` وبالفهارس الستة القائمة. لا يحتاج هذا الإصدار إلى حذفها أو إنشائها حتى تعمل الواجهات المصلحة. تفيد توثيقات Firebase بأن حالة بناء الفهرس قد تستغرق دقائق وأن Console تظهر حالتها، لكن وجود فهرس وحده لا يثبت أن الموقع المنشور يستعمل إصدار JavaScript الموافق له. [3]

## تعديل القواعد الخاص بـ PIN

لا تسمح القاعدة الجديدة إلا لصاحب ملف محمي (`isProtected: true`) بتغيير `securityPin`، إلى جانب الاسم والهاتف ووقت التحديث. وعند تغيير PIN تفرض القاعدة نص SHA-256 من 64 خانة سداسية. بقيت الأدوار والحالة والبريد وخصائص الحماية الأخرى غير قابلة للتعديل الذاتي. هذا التغيير لا يقرأ أو يغير أي بيانات مخلفات أو مركبات أو عهد.

## رسائل التشخيص

عند فشل تحميل مخالفات، تعرض واجهات المدير الآن رمز Firestore المختصر فقط، مثل `permission-denied` أو `failed-precondition`، ولا تكشف الرسالة الخام أو روابط أو تفاصيل قد لا تخص المستخدم العادي. يعتمد التصرف الصحيح على الرمز:

| الرمز | المعنى العملي | الإجراء |
|---|---|---|
| `permission-denied` | قواعد Firestore المنشورة أو مشروع Firebase لا يطابقان الحزمة. | انشر ملف `firestore.rules` كاملاً في المشروع الصحيح ثم أعد تسجيل الدخول. |
| `failed-precondition` | ما زال المتصفح يستخدم إصداراً قديماً، أو توجد سياسة فهرسة غير معتادة في المشروع. | ارفع الحزمة كاملة، نفذ تحديثاً قسرياً، ثم راجع الحقول المستثناة من الفهرسة فقط إن استمر الخطأ. |
| `unavailable` | مشكلة مؤقتة في الشبكة أو الخدمة. | أعد المحاولة لاحقاً. |
| رمز آخر | لا يُستنتج سببه بأمان من الواجهة وحدها. | احتفظ بالرمز وراجع Console المشروع قبل تغيير البيانات أو القواعد. |

## عناصر لم تتغير ولم تُمس

بقيت المطابقة التلقائية وفق اللوحة ووقت المخالفة وفترة العهدة، وسجل التسوية، وحظر تعديل المخالفة للمستخدم العادي، وبطاقات الطي، وزر الإضافة السياقي من السيارة أو المستخدم، وصيغة وقت الإمارات الإنجليزية. لم تُجر أي عملية ترحيل أو حذف أو تعديل على بيانات Firebase الفعلية.

لاحظت المقارنة أن `index(2).html` يشير إلى ملفات PWA (`manifest.json` و`icon.png` و`sw.js`) غير موجودة في حزمة الإصدار النظيفة. لم يعدل هذا التدقيق تلك الملفات لأنها خارج نطاق عطل المخالفات ولأن النسخة الأمنية الأساسية لا تقدم مقابلاً موثقاً لها؛ يبقى ذلك تحسيناً مستقلاً منخفض الخطورة وليس سبباً للمشكلة الحالية.

## فحوص الحزمة المنفذة

| الفحص | النتيجة |
|---|---|
| تركيب جميع ملفات JavaScript عبر `node --check` | ناجح. |
| تحليل `firestore.indexes.json` | ناجح. |
| توازن أقواس `firestore.rules` | ناجح. |
| مراجعة أن الاستعلامات المقيدة لم تعد ترسل `orderBy` أو `startAfter` | ناجح. |
| مراجعة أن قواعد PIN وإجراء الحفظ متوافقان | ناجح. |
| اختبار حي على مشروع Firebase الخاص بالمستخدم | لم ينفذ، حمايةً للبيانات الحية. |

## المراجع

[1]: https://firebase.google.com/docs/firestore/security/rules-query "Firebase: Securely query data"
[2]: https://firebase.google.com/docs/firestore/query-data/index-overview "Firebase: Index types in Cloud Firestore"
[3]: https://firebase.google.com/docs/firestore/query-data/indexing "Firebase: Manage indexes in Cloud Firestore"
