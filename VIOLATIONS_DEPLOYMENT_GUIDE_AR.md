# دليل نشر إصلاح المخالفات والتسوية

## ما الذي يصلحه هذا الإصدار

تعالج هذه الحزمة فشل صفحات **My Violations** وفلاتر المدير وسجل المخالفات في بطاقتي السيارة والمستخدم من دون حذف أي مخالفة أو تغيير منطق المطابقة التلقائية أو التسوية. بقيت صفحة **All** تستخدم ترتيب Firestore المتدرج، أما الفلاتر والسجلات المرتبطة فصارت تستخدم استعلامات مساواة آمنة ثم ترتب النتائج في المتصفح وتعرض **10 سجلات** في كل مرة. لذلك لا يتوقف عمل هذه المسارات على صحة أو حالة الفهارس المركبة المنشورة حالياً.

> لا تحذف الفهارس الستة الموجودة في مشروعك. لم تعد مطلوبة لتشغيل واجهة هذه الحزمة، لكنها لا تضر البيانات ويمكن الاحتفاظ بها لتطوير مستقبلي أو لاستعلامات إدارية أخرى.

## الملفات الواجب نشرها

ارفع **كل ملفات الواجهة** من الحزمة بوصفها مجموعة واحدة، ولا تكتفِ برفع `violations.js` وحده. يضمن ذلك عدم تشغيل المتصفح خليطاً من وحدات قديمة وجديدة.

| الملف | الغرض |
|---|---|
| `index(2).html` | واجهة النظام والتبويبات. أعد تسميته إلى `index.html` فقط إذا كانت استضافتك تستخدم هذا الاسم. |
| `app.js`، `cars.js`، `members.js`، `violations.js` | التنقل، البطاقات، ملف المدير المحمي، ومنطق المخالفات. |
| `utils.js` | رسائل الأخطاء ودعم PIN المتوافق للمدير المحمي. |
| `firebase.js`، `logs.js`، `requests.js`، `search.js`، `stats.js` | وحدات النظام المشتركة التي يجب أن تبقى متزامنة مع الواجهة. |
| `style.css` | التنسيق الحالي للبطاقات والسجلات. |
| `firestore.rules` | قواعد Firestore؛ تنشر في Firebase Console ولا ترفع كملف موقع عادي. |
| `firestore.indexes.json` | سجل الفهارس الحالية للاحتفاظ به، ولا يلزم تغيير Console بسببه في هذا الإصدار. |

## خطوات النشر الآمن

1. احتفظ بنسخة من الموقع المنشور حالياً ومن نص قواعد Firestore الحالية.
2. ارفع ملفات الواجهة كلها من هذه الحزمة إلى مجلد الموقع نفسه، مع استبدال الملفات الموجودة.
3. في **Firebase Console → Firestore Database → Rules**، استبدل القواعد كاملةً بمحتوى `firestore.rules` من هذه الحزمة ثم اختر **Publish**.
4. لا تنشئ فهارس جديدة ولا تحذف الفهارس القائمة لأجل هذا الإصلاح. إذا كانت الفهارس السابقة ظاهرة بحالة **Enabled** فيمكن تركها كما هي.
5. افتح الموقع في نافذة خاصة أو نفذ تحديثاً قسرياً للمتصفح، ثم سجّل الخروج وسجّل الدخول. هذه الخطوة مهمة لمنع استخدام نسخة JavaScript مخزنة مؤقتاً.

> ظهور رسالة `Access to violations was denied...` للمدير بعد نشر الموقع يعني غالباً أن قواعد Firestore المنشورة ليست نص `firestore.rules` المرفق أو أن المشروع/الاستضافة ليسا المشروعين المتوقعين. لا تغيّر الفهارس عند ظهور هذه الرسالة؛ انشر القواعد كاملة أولاً.

## اختبار القبول بعد النشر

| الاختبار | الخطوات | النتيجة السليمة |
|---|---|---|
| قائمة المدير الكاملة | افتح **Violations** ثم **All**. | تعرض أحدث 10 سجلات ثم زر **Load More** عند الحاجة. |
| فلاتر المدير | جرّب Linked وNo Car وNo Assignment وReview Required وUnsettled وSettled. | تعمل كل الفلاتر من دون طلب فهرس مركب، وتظهر النتائج الأحدث أولاً وبواقع 10 سجلات. |
| سجل سيارة | افتح زر **Violations** داخل بطاقة سيارة. | المدير يرى كل مخالفات السيارة، والمستخدم العادي يرى تلقائياً مخالفاته المرتبطة به لتلك السيارة فقط. |
| سجل مستخدم | افتح زر **Violations** داخل بطاقة مستخدم كمدير، ثم اختبر **My Violations** بحساب مستخدم عادي. | لا تظهر إلا السجلات المصرح بها لكل دور، مع **Load More** كل 10 سجلات. |
| PIN المدير المحمي | سجّل الدخول بالحساب ذي `isProtected: true` وافتح **Members → Edit My Profile**. | تطلب الصفحة كلمة المرور وPIN الحالي إن كان محفوظاً، وتتيح تغيير PIN. إذا كان `securityPin` فارغاً في الملف القديم، يطلب النموذج إنشاء PIN جديد مرة واحدة. |
| التسوية | افتح مخالفة غير مسوّاة كمدير ثم اختر طريقة وأدخل ملاحظة. | تنتقل إلى Settled وتحتفظ بالبيانات ووقت ومنفذ التسوية. |

## قواعد الإدخال التي لم تتغير

يُدخل وقت المخالفة بتوقيت الإمارات **GMT+4** وبالصيغة الإنجليزية `YYYY-MM-DD HH:MM` مثل `2026-08-24 09:30`. لا تستخدم ص/م أو الأرقام العربية داخل الحقل. يعتمد الربط على اللوحة الكاملة وفترة العهدة التي تحتوي وقت الحدث، وليس على المستخدم الحالي فقط.

## ملاحظة حماية مهمة لملف المدير

كلمة مرور Firebase وإعادة التحقق بها هي الحماية الأساسية للمدير المحمي. أعيد دعم PIN رباعي الأرقام بوصفه **تأكيداً إضافياً متوافقاً** مع الحسابات القديمة، ويخزن في شكل SHA-256 عند تغييره. ضمن خطة Spark وواجهة متصفح فقط، لا يعد PIN رباعي الأرقام عاملاً مستقلاً بالقوة نفسها لكلمة المرور؛ لذلك لا ينبغي اعتباره بديلاً عنها.

لا تفتح تعديل البريد من Firestore مباشرة. بريد الدخول موجود في Firebase Authentication، وتغييره من واجهة Firestore فقط يسبب عدم اتساق بين هوية الدخول وملف المستخدم. لم يكن تعديل البريد جزءاً من النسخة الأمنية الأساسية أو النسخة الأخيرة، ولذلك أبقيته معطلاً في هذه الحزمة بدلاً من إضافة تدفق غير ذري قد يعطل تسجيل الدخول.

## حدود الاختبار

تم فحص تركيب JavaScript، وصيغة JSON، ومسارات الاستيراد، وتوازن أقواس القواعد. لم يجر اختبار حي على مشروع Firebase الخاص بك ولم تُنشأ أو تُعدّل بيانات فعلية. لذلك نفّذ جدول اختبار القبول أعلاه باستخدام مخالفة تجريبية قبل إدخال أي سجل تشغيلي مهم.
